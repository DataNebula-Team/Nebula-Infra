# .env.example — SRV-KAFKA
#
# Este servicio NO requiere archivo .env. El docker-compose.yml corre
# en modo PLAINTEXT sin autenticación (válido para el lab, tráfico
# interno) y todos sus valores de configuración (CLUSTER_ID, retención,
# listeners) están hardcodeados directo porque ninguno es sensible.

