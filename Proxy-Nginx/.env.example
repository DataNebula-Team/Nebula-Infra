# .env.example — SRV-PROXY
#
# Este servicio (Nginx Proxy Manager) NO requiere archivo .env.
# El docker-compose.yml no declara env_file ni variables de entorno
# desde .env — toda su configuración vive en el volumen ./data,
# administrado desde la UI web (puerto 81).
